import 'package:flutter/material.dart';

class CharacterSpriteWidget extends StatelessWidget {
  const CharacterSpriteWidget({super.key});

  @override

  Widget build(BuildContext context) {
    return const Text("Character Sprite Widget");
  }
}